import { MissionControl } from '@/features/workspace/components/mission-control';

export const metadata = {
  title: 'Mission Control | HibirDev AI',
  description: 'Live compute telemetry and agent execution console.',
};

export default function MissionControlPage() {
  return <MissionControl />;
}
