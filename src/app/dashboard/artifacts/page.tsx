import { ArtifactsRegistry } from '@/features/artifacts/components/artifacts-registry';

export const metadata = {
  title: 'Artifacts Registry | HibirDev AI',
  description: 'Explore compiled product specs, architecture blueprints, and code outputs.',
};

export default function DashboardArtifactsPage() {
  return <ArtifactsRegistry />;
}
