// This file serves as the public API for the 'theme' package.
// We export the centralized styling constants so they can be consumed by other packages or components.

export { colors } from './colors';
export { typography } from './typography';
